<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/ll_tracks"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/tv_tracks_margin_top"
            android:layout_marginBottom="@dimen/tv_tracks_margin_bottom"
            android:layout_marginStart="@dimen/tv_tracks_margin_start"
            android:textSize="18sp"
            android:text="@string/tracks"
            style="@style/PrimaryTextView.Bold"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_tracks"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingTop="@dimen/rv_tracks_padding_top"
            android:paddingHorizontal="@dimen/rv_tracks_padding_hor"
            android:clipToPadding="false"/>

    </LinearLayout>

    <TextView
        android:id="@+id/tv_error"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:textSize="14sp"
        android:text="@string/loading_error"
        android:visibility="gone"
        style="@style/PrimaryTextView.Regular" />

    <TextView
        android:id="@+id/tv_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:textSize="14sp"
        android:text="@string/loading_empty"
        android:visibility="gone"
        style="@style/PrimaryTextView.Regular" />

</FrameLayout>